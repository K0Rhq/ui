---
import { getCollection } from "astro:content";

const items = await getCollection("previews");
console.log("ITEMS", items);
const firstItem = items.find((item) => !item.data.directory);
return Astro.redirect(`/playground/${firstItem.id}`);
---
