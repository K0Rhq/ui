---
import { getCollection } from "astro:content";

const items = await getCollection("previews").then((c) =>
    c.filter((item) => !item.data.directory),
);
console.log(items);
return Astro.redirect(`/playground/${items[0].id}`);
---
