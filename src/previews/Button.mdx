
---
title: "Button"
variants:
  text:
    default: "Button"
    options:
      - "Button"
      - "asd"
---

import Button from "@/components/base/Button.svelte"

<Button {...props} client:load>{props.text}</Button>
