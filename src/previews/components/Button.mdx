---
title: "Button"
variants:
  variant:
    default: "primary"
    options:
      - "primary"
      - "secondary"
      - "success"
      - "warning"
      - "danger"
---

import Button from "@/components/base/Button.svelte"

<Button variant={props.variant} client:load>Button</Button>
